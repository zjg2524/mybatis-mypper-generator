<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
mapper接口代理实现编写规则:
	1. 映射文件中namespace要等于接口的全路径名称
	2. 映射文件中sql语句id要等于接口的方法名称
	3. 映射文件中传入参数类型要等于接口方法的传入参数类型
	4. 映射文件中返回结果集类型要等于接口方法的返回值类型
 -->
<mapper namespace="com.jmeter.demo.dao.UserMapper2">

	<resultMap id="userAndOrders" type="com.jmeter.demo.entity.Users">
		<id property="sid" column="sid"/>
		<result property="sname" column="sname" />
		<result property="pwd" column="pwd" />
		<result property="sage" column="sage" />

		<collection property="orders" javaType="com.jmeter.demo.entity.Orders">
			<id property="id" column="id"/>
			<result property="name" column="name" />
			<result property="date" column="date" />
			<result property="desc" column="desc" />
		</collection>
	</resultMap>
	<select id="selectUserAndOrderAll" parameterType="int" resultMap="userAndOrders" >
		select * from users a ,orders b where
		a.sid = b.sid
		and a.sid=#{id}
	</select>

	
</mapper>